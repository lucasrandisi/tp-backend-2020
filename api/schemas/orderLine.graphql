type OrderLine {
	id: ID!
	item: Item!
	order: Order!
	quantity: Int
}

type Query {
	lines: [OrderLine!]
	line(id: ID!): OrderLine
}

type Mutation {
	createLine(lineInput: LineInput!): OrderLine!
	deleteLine(id: ID!): Int!
}

input LineInput {
	orderId: ID!
	itemId: ID!
	quantity: Int!
}
